spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test_db?useSSL=false&serverTimezone=UTC
    username: root
    password: ws.jBhqq%8#h
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      jdbc:
        batch_size: 1000
      order_inserts: true
      order_updates: true
      batch_versioned_data: true
      ddl-auto: none  # 你不想自動創建資料表就寫 none（create / update / validate）
    show-sql: false
    properties:
      hibernate:
        format_sql: false

        